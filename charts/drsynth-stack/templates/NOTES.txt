drsynth-stack has been deployed.

BFF (samuraibff)
  Service: {{ include "drsynth-stack.fullname" . }}-bff
  Type: {{ .Values.bff.service.type }}

{{- if eq .Values.bff.service.type "NodePort" }}
  URL: http://localhost:{{ .Values.bff.service.nodePort }}
{{- else }}
  Use `kubectl port-forward svc/{{ include "drsynth-stack.fullname" . }}-bff 8000:8000`
{{- end }}

Persistor
  Service: {{ include "drsynth-stack.fullname" . }}-persistor
  Use `kubectl port-forward svc/{{ include "drsynth-stack.fullname" . }}-persistor 8010:8010` if you need it.

rtservice (gRPC)
  Service: {{ include "drsynth-stack.fullname" . }}-rtservice
