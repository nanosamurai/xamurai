"""Generated protobuf/gRPC modules.

We keep generated python code under `proto_gen/` and import it as a proper
package (e.g. `from proto_gen import stream_pb2`).

If you regenerate code with `grpc_tools.protoc`, ensure imports inside
`stream_pb2_grpc.py` remain package-relative.
"""
