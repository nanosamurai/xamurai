# Integration-test dependencies including ML stack.
#
# WARNING:
# - This is intended for on-demand CI experiments or self-hosted runners.
# - It can be slow to install.
# - Torch itself is installed separately in the workflow to control CPU vs CUDA wheels.

-r requirements.integration.txt

# rtservice integration deps
faster-whisper==1.2.0
pyannote-audio==4.0.1
omegaconf==2.3.0
hydra-core==1.3.2

# whisperx integration deps
whisperx==3.3.4

# test_realtime_asr_grpc imports librosa when it needs to resample
librosa==0.11.0
