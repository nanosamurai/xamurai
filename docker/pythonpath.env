# Common PYTHONPATH for drsynth monorepo containers
# Keep /app itself on PYTHONPATH so `import proto_gen` works.
PYTHONPATH=/app:/app/shared/src:/app/rtservice/src:/app/whisperx_worker/src:/app/recorder_worker/src:/app/finalizer_worker/src
